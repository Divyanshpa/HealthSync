# Shared PostgreSQL Database Connection String
# Replace with your shared cloud database connection string (Neon / Supabase / ElephantSQL)
DATABASE_URL="postgresql://username:password@ep-cloud-instance.region.aws.neon.tech/digital_health_db?sslmode=require"
